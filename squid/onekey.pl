#!/usr/bin/perl
$|=1;
$pid = $$;
while (<>) {
chomp $_;
        @@@@
                print "$_\n";
        }
        # match
        elsif(m/.*\.apk/i){
                print "$_\n";
        }
        else{
                ####
        }
}
